# use command to setup git line endings
git config --global core.eol lf
git config --global core.text auto

#this will convert all crlf into lf

# for more information look into 
man gitattributes